name: Java CI with Maven

on:
  push:
    branches:
      - main
  pull_request:
    branches: 
      - main

jobs:
  build_test:
    uses: ./.github/workflows/templates/build_test.yml
    with:
      project_name: HandInSubmission